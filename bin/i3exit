#!/usr/bin/env bash
case "$1" in
lock)
	i3lock
	;;
logout)
	rofirm "Are you sure you want to logout?"	"i3-msg exit"
	;;
suspend)
	rofirm "Are you sure you want to suspend?" "systemctl suspend"
	;;
hibernate)
	rofirm "Are you sure you want to hibernate?" "i3lock"
	systemctl hibernate
	;;
reboot)
	rofirm "Are you sure you want to reboot?" "systemctl reboot"
	;;
shutdown)
	rofirm "Are you sure you want to shutdown?" "systemctl poweroff"
	;;
*)
	echo "Usage: i3exit {logout|suspend|hibernate|reboot|shutdown}"
	exit 2
	;;
esac
