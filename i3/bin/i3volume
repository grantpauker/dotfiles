#!/usr/bin/env bash
source ~/.theme/shell
if sc isjack; then
	icon=""
else
	icon=""
fi
if [ "$(< ~/.system/max-volume)" != "--allow-boost" ]; then
	COLOR="$base0C"
else
	COLOR="$base0F"
fi
if sc ismute; then
	printf "$icon ––\n\n$COLOR"
else
	printf "$icon $(sc getvol)\n\n$COLOR"
fi
