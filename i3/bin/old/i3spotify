#!/usr/bin/env bash
source ~/.theme/shell
if ! pgrep -x "spotify" &>> /dev/null ; then
	exit
fi
if [[ "$(spotify-now -p pause)" == "pause" ]]; then
	printf " "
else
	printf " "
fi
printf "$(spotify-now -i "\"%title\" by %artist")\n\n$base0C" 
